import React from "react";
import styled from "styled-components";

const Info = ({ data }) => {
  return <Container>dfsfsd</Container>;
};

const Container = styled.div`
  width: 50%;
`;

export default Info;
